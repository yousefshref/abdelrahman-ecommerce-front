- compelete user profile ✅
- navbar for all users types ✅
- sales pending page








-------------------------------
if client (not logged in) -> appear login
if logged in:
    if client:
        appear profile
    if sales:
        appear dashboard
